<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MauiSourceGenAsyncRelayCommand.ViewModels"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="MauiSourceGenAsyncRelayCommand.MainPage"
             x:DataType="local:MainViewModel">

    <ContentPage.Resources>
        <toolkit:InvertedBoolConverter x:Key="InvertedBoolConverter" />
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <!-- Row 1: standard compiled binding from ViewModel -->
            <HorizontalStackLayout Spacing="16">
                <Button Text="First"
                        Command="{Binding FirstCommand}" />
                <CheckBox IsEnabled="{Binding FirstCommand.IsRunning, Converter={StaticResource InvertedBoolConverter}}" />
            </HorizontalStackLayout>

            <!-- Row 2: inline x:DataType on the binding using the full CLR name -->
            <HorizontalStackLayout Spacing="16">
                <Button Text="Second"
                        Command="{Binding SecondCommand}" />
                <CheckBox IsEnabled="{Binding SecondCommand.IsRunning, Converter={StaticResource InvertedBoolConverter}, x:DataType=CommunityToolkit.Mvvm.Input.IAsyncRelayCommand}" />
            </HorizontalStackLayout>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
